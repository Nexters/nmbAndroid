<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#5d5d5d</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NoTitle" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="colorPrimaryDark">@android:color/transparent</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="FacebookLoginButton">
        <item name="android:textSize">16sp</item>
        <item name="android:layout_width">193.3dp</item>
        <item name="android:layout_height">46.7dp</item>
        <item name="android:background">@drawable/facebook_radius_rect_fill</item>
    </style>

    <style name="FragmentTabText">
        <item name="android:fontFamily"></item>
    </style>

    <style name="dialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:background">@null</item>
        <item name="android:divider">@null</item>
        <item name="android:textColorPrimary">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">@null</item>
    </style>
</resources>
