<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nexters.naemambo.naemambo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Notile" />
        <activity android:name=".util.BaseActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Notile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="84cd339727a86e3c0bc0ad74e069573d33ea03d6" />

        <activity android:name=".MyboxActivity" />
        <activity
            android:name=".OpenSourceActivity"
            android:theme="@style/Notile" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".CreditActivity"/>
    </application>

</manifest>